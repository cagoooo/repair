# 校園報修系統 (Campus Repair System)

一個基於 React + Vite + Firebase 的校園設備報修系統。

## 🌟 功能特色

- **AI 自動辨識地圖**：清晰圖片優先、PDF 可選，透過受權限與額度保護的 Google Vision 後端辨識教室編號與名稱。
- **新學期安全換版**：以固定教室編號銜接報修歷史，套用前顯示差異並自動備份舊配置。
- **智慧名稱聚合**：獨家遞迴連鎖演算法，能精準抓取分散的班級文字。
- **專業級標籤排版**：編號與名稱垂直堆疊，支援小空間溢出顯示，極具美感。
- **互動式地圖報修**：直觀點擊教室即可報修。
- **RWD 響應式設計**：支援手機、平板與桌機，介面繽紛且操作流暢。
- **即時狀態更新**：整合 Firebase Firestore，報修狀態即時同步。
- **權限管理**：
  - **一般使用者**：僅可報修。
  - **管理員**：可管理報修單、編輯地圖、設定通知、執行 AI 掃描。
- **LINE 通知**：新報修會自動通知對應組長，管理員也能針對未結案案件再次發送提醒。
- **雲端設定同步**：系統設定儲存於雲端，維護更便利。

## 🚀 快速開始

### 1. 安裝依賴

```bash
npm install
```

### 2. 設定環境變數

請複製 `.env.example` 為 `.env` 並填入 Firebase 前端設定。Vision 憑證由 Cloud Functions 的服務帳號管理，不可放進前端環境變數：

```ini
# Firebase Config
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_bucket.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

### 3. 以開發模式執行

```bash
npm run dev
```

### 4. 建置正式版

```bash
npm run build
```

### 5. 部署地圖 OCR 後端

```bash
firebase deploy --only functions:repair-map --project=smes-e1dc3 --account=ipad@mail2.smes.tyc.edu.tw
```

## 🛠️ 技術堆疊

- **前端框架**: React 19
- **建置工具**: Vite
- **AI 服務**: Google Cloud Vision API
- **樣式**: CSS (Variables, Gradients, Glassmorphism)
- **後端服務**: Firebase (Auth, Firestore, Storage, Hosting)
- **部署**: GitHub Pages / Firebase Hosting

## 📝 版本紀錄

- **v0.9.0** (2026/04/20):
  - 🐛 **修正「篩選中」卡片 UI 破版**：v0.8.9 列表頁統計卡片被點擊後，整張卡片變成紫色巨塊，內容全被蓋住。
  - **原因**：既有的 `.stat-card::before` 已被用作全高度漸層覆蓋層（`height: 100%`），我用 `.stat-card.clickable.active::before` 添加徽章內容時，CSS 級聯繼承了 `height: 100%` 與 `left: 0`，導致紫色背景把整張卡片填滿。
  - **修正**：改用真實 DOM 元素 `<span class="stat-active-badge">` 取代 `::before` 偽元素，徹底避開級聯衝突。
  - **順便升級 UI**：
    - 徽章改為紫藍漸層、pulse 動畫（光暈循環呼吸）
    - 卡片啟用時改為淡紫色漸層背景 + 紫色外框，不再死板
    - 同步重構 AdminDashboard 與 RepairList 兩處程式碼為 data-driven `.map()`，方便後續維護

- **v0.8.9** (2026/04/20):
  - 🎯 **列表頁統計卡片也支援點擊快速篩選**：延伸 v0.8.8 管理後台的點擊篩選機制到「列表」頁面，全站使用體驗一致。
    - ⏳ 待處理 / 🔄 處理中 / ✅ 已完成 → 點擊套用對應 status 篩選
    - 🔥 緊急案件 → 點擊套用 priority=urgent 篩選（未完成）
    - 新增 `filter.priority` state 與列表錨點 `listSectionRef`
    - 點擊後平滑 `scrollIntoView` 捲動到列表區
    - 被點擊的卡片顯示「✓ 篩選中」紫色徽章 + 外框高亮
    - 頂部顯示啟用的篩選狀態列（條件 chips + 結果筆數 + 一鍵清除）

- **v0.8.8** (2026/04/20):
  - 🎯 **儀表板統計卡片可點擊篩選**：點擊管理後台的任一統計卡片即自動套用篩選並平滑捲動到報修列表。
    - ⏳ 待處理 → 只看 `pending`
    - 🔧 處理中 → 只看 `in_progress`
    - ✅ 已完成 / ⚡ 平均完修 → 只看 `completed`
    - 🔥 緊急案件 → 只看優先度 `urgent`（未完成）
    - **視覺反饋**：被點擊的卡片顯示「✓ 篩選中」紫色徽章 + 外框高亮
    - **篩選狀態列**：列表上方顯示目前啟用的篩選條件 + 結果筆數 + 一鍵清除按鈕
    - Hover 時右上角浮現 👆 點擊提示

- **v0.8.7** (2026/04/19):
  - 📋 **列表頁同步套用角色切換**：管理員進入「列表」頁面時，也會自動彈出角色選擇器（若尚未選過），並依角色過濾顯示的報修卡片。
    - 列表頂端新增「目前檢視」資訊列，顯示當前角色（🖥️ 資訊組長 / 🔧 事務組長 / 📊 全部檢視）
    - 右側提供「🔄 切換角色」按鈕，可隨時切換
    - 統計卡片（待處理/處理中/已完成/緊急）依角色過濾，數字準確反映該角色工作量
    - 與管理後台共用同一個 `adminRole` state，兩邊切換立即同步

- **v0.8.6** (2026/04/19):
  - 🔄 **角色卡新增「處理中」徽章**：角色選擇器的每張卡片除了 ⏳ 待處理、📂 總數之外，再加上 🔄 處理中的數量，管理員一眼就能看出工作進度全貌（藍色徽章，與其他狀態區分）。

- **v0.8.5** (2026/04/19):
  - 👥 **管理員角色切換 (Role-Based Admin View)**：進入管理後台時自動彈出角色選擇器，讓資訊組長與事務組長各看各的報修，互不干擾。
    - **三種檢視模式**：🖥️ 資訊組長（只看 IT 類別）、🔧 事務組長（只看 GENERAL 類別）、📊 全部檢視（看所有）
    - **智慧提示**：每張角色卡顯示該角色的待處理數與總數，一眼看出哪邊緊急
    - **localStorage 記憶**：選擇後記住，下次進入直接套用（不再彈出）
    - **隨時切換**：後台右上角 Admin 徽章改為可點擊的角色切換按鈕
    - **資料過濾**：整個儀表板（統計卡、圓餅圖、列表）全部依角色過濾

- **v0.8.4** (2026/04/19):
  - ⏳ **地圖載入進度條 (Map Loading Indicator)**：首次進入或重新整理頁面時，在地圖尚未出現前，顯示動畫進度條讓使用者知道系統正在讀取，避免誤以為網頁當掉。
    - 新增 `MapLoadingIndicator` 元件，含漸層進度條、shimmer 光影動畫
    - 5 段式階段文字：連線雲端 → 載入地圖 → 讀取報修 → 即將完成 → 整理中
    - 進度曲線：前段快速、後段漸緩（最多 95%），符合使用者預期心理
    - 🏫 建築物跳動 + 📍 圖釘落下的雙圖示動畫，提升視覺趣味
    - 貼心提示：「首次載入可能需要數秒，請稍候不要重新整理」

- **v0.8.3** (2026/04/19):
  - 🔴 **報修徽章貼齊標籤 (Badge Hugs Label)**：將紅色報修數量徽章從「教室右上角」改為「標籤框右上角」，徹底解決小教室區塊被徽章蓋住編號的問題。
    - 新增 `.room-label-wrapper` 容器，讓徽章相對於標籤定位
    - 徽章改為藥丸型（pill shape），高度 9-16px，支援兩位數 `99+`
    - 位置：`transform: translate(45%, -45%)`，露出一半在標籤框外不遮擋文字
    - 尺寸分層：桌機 16px、手機 12px、iPhone 10px、iPhone SE 9px

- **v0.8.2** (2026/04/19):
  - 📱 **教室標籤再瘦身 (Label Extra Compact)**：進一步壓縮手機端教室編號按鈕與字體，解決重疊擠壓問題。
    - 768px 以下：字體 5-9px、padding 0 2px、邊框 0.5px、徽章 14px
    - 480px 以下：字體 4-7px、padding 0 1px、徽章 11px
    - 375px 以下（iPhone SE 等）：字體 3.5-6px、徽章 10px
    - 加入 `letter-spacing: -0.3px ~ -0.5px` 壓縮字距，同寬度可容納更多字元

- **v0.8.1** (2026/04/19):
  - 📱 **手機端縮放自動重置 (Mobile Zoom Auto-Reset)**：使用者在手機上放大地圖找教室後點擊，開啟報修申請表單時會自動把頁面縮放重置為 100%，再也不會看到被放大扭曲的表單。透過動態改寫 `<meta name="viewport">` 觸發 iOS Safari 的 viewport 重算，0.35 秒後還原原設定讓使用者仍可自由縮放地圖。

- **v0.8.0** (2026/04/19):
  - 📸 **照片上傳系統大翻新 (Photo Upload Overhaul)**：
    - **修正 iPhone 上傳失敗**：壓縮格式由 WebP 改為 JPEG（iOS Safari Canvas 對 WebP 編碼支援不全），並支援 HEIC/HEIF 副檔名偵測。檔案大小上限放寬至 20MB，壓縮後控制在 300KB。
    - **管理員後台上傳照片**：展開報修詳情即可補上遺漏的現場照片，每筆最多 5 張，附即時進度提示。
    - **管理員刪除照片**：滑鼠 hover 照片顯示紅色 ✕ 按鈕，同步清除 Firebase Storage 檔案與 Firestore 紀錄。
    - **部署 Storage Rules**：修正長期存在的 403 unauthorized 問題，正式允許匿名使用者上傳照片至 `repair-images/`。
    - **錯誤處理強化**：單張照片失敗不影響其他照片與報修單提交。
  - 🖨️ **列印報修單加入處理備註**：列印版面現在會帶入完整的管理員備註（作者、時間、內容），方便紙本存查。
  - 📱 **手機端地圖按鈕優化 (Mobile Map)**：
    - 768px 以下：字體 6-10px、padding 1px 3px、邊框/陰影更細緻。
    - 480px 以下（iPhone 等）：字體 5-8px、padding 0 2px、徽章縮為 13px。
    - 解決教室標籤在小螢幕上嚴重重疊的問題，低縮放瀏覽時字體自動變得更迷你。

- **v0.7.5** (2026/04/14):
  - 📢 **免登入說明優化 (Guest Access Clarity)**：為消除一般使用者「必須登入才能報修」的誤解，在地圖頁面新增了醒目的提示橫幅，並將設定頁面的登入按鈕明確標註為「管理員登入」，強調報修功能對所有訪客開放。

- **v0.7.4** (2026/04/14):
  - 🛠️ **新增「系統維護」工具 (Maintenance Tools)**：於設定頁面新增「強制更新與清除快取」按鈕。此功能會自動註銷 Service Workers 並排空 Cache Storage，解決因瀏覽器過度快取導致無法即時看到最新樣式或功能的問題。

- **v0.7.3** (2026/04/14):
  - 📏 **標籤排版最終修正 (Label Layout Final Fix)**：採用強規式 CSS 策略（`width: max-content` 與 `!important` 權限），徹底解決在某些行動裝置或極窄空間下，編號仍會發生斷行的問題。

- **v0.7.2** (2026/04/14):
  - 📏 **標籤版型強規化 (Label Layout Standardization)**：強制地圖標籤以單行 (Single Row) 呈現，解決部分包含空格的教室編號（如 C21 1）在窄小的手機視圖下發生非預期分行的問題，確保視覺統一與美觀。

- **v0.7.1** (2026/04/14):
  - 🎨 **地圖標籤簡約化 (Minimalist Map Labels)**：移除了地圖上的教室名稱顯示，僅保留教室編號。這大幅減少了地圖標籤對平面圖底圖的遮擋，讓原有的地圖資訊更易於辨識。
  - 🔍 **視覺微調**：進一步縮小標籤體積並優化對比度。

- **v0.7.0** (2026/04/14):
  - 📱 **手機版地圖深度優化 (Mobile Map Optimization)**：
    - **縮放感知標籤 (Adaptive Labels)**：地圖縮小時自動隱藏詳細名稱，放大後才顯示完整教室資訊，解決標籤遮擋問題。
    - **玻璃擬態 UI (Glassmorphism)**：標籤底色改為具通透感的磨砂玻璃質感，視覺上更輕盈且不遮擋地圖細節。
    - **連選反饋強化**：選中教室後增加藍色光暈與邊框，解決觸控點選時的定位迷思。
    - **排版邏輯修復**：修正窄長房間內標籤文字垂直破碎的問題，確保橫向閱讀體驗。
    - **操作導覽**：新增移動端縮放手勢引導文字。

- **v0.6.1** (2026/02/25):
  - 🛡️ **權限安全性修復 (Security Patch)**：重新設計管理員驗證邏輯，解決動態管理員無法讀寫 `system/adminConfig` 的權限不足問題。
  - 📝 **系統日誌優化**：改善 Firestore 權限錯誤偵測。

- **v0.6.0** (2026/02/19):
  - 📊 **數據分析儀表板 2.0**：新增平均維修時間統計、熱門報修熱點圖與報修王排行榜。
  - 📲 **PWA 體驗升級**：重新設計安裝提示 UI，修復 Service Worker 快取錯誤並增加手動安裝觸發。

- **v0.5.0** (2026/02/18):
  - 👥 **動態管理員 (Dynamic Admin)**：支援從 Firestore 動態載入管理員名單，不再需要修改程式碼即可新增管理員。
  - 📢 **通知分流 (Notification Separation)**：區分「資訊組」與「事務組」的 LINE 通知 Token，確保訊息準確傳達給負責單位。
  - 🗑️ **批次刪除 (Batch Delete)**：管理後台新增批次選取與刪除功能，大幅提升舊資料整理效率。
  - ⚙️ **UX 優化**：新增 LINE Token 同步功能與詳細設定指引。


- **v0.4.0** (2026/02/12):
  - 📡 **全方位離線支援**：實作 Firestore 離線持久化與 IndexedDB 圖片暫存機制，解決網路死角報修痛點。
  - 🔄 **智能自動同步**：偵測連線恢復後自動執行背景同步，包含圖片壓縮與 Line 通知補發。
  - 🆘 **地圖區域動態**：為「緊急」報修區域新增動態擴散波紋與紅光閃爍效果，大幅強化警示視覺。
  - 💾 **離線 UI 引導**：提交時若斷網，成功畫面會自動切換為本地存儲標記，提供準確的操作回饋。

- **v0.3.1** (2026/02/12):
  - 🎨 **頁尾視覺重盤**：導入 Aurora UI 流動漸層與 Glassmorphism 磨砂玻璃效果，大幅提升質感。
  - 🧩 **整合圖例設計**：將地圖狀態圖例整合至頁尾，並支援智慧 RWD 佈局，解決手機版擁擠問題。
  - 💓 **動態體驗**：為狀態指示燈與版權圖標加入細微動效，提升介面生命力。

- **v0.3.0** (2026/02/12):
  - 🚀 **UX 完整反饋**：新增提交成功慶祝畫面與報修摘要，提供更明確的操作回饋。
  - 📷 **列印功能強化**：報修單現在支援夾帶 1-3 張現場照片，並優化了列印格線排版。
  - 📱 **RWD 全面修復**：修正成功畫面在小螢幕下的裁切問題，支援捲動並優化手機端視覺。
  - ⚙️ **提交流程優化**：解耦表單關閉與提交邏輯，確保使用者能完整閱覽報修結果。

- **v0.2.0** (2026/02/12):
  - 🤖 **AI 大升級**：整合 Google Cloud Vision，支援自動標記教室區域。
  - 🧲 **名稱聚合優化**：新增「連鎖磁吸」演算法，找回破碎的班級文字（如一年 1 班）。
  - 🎨 **UI 全面美化**：地圖標籤改為堆疊式設計，大幅提升高解析與小螢幕下的閱讀性。
  - 🧹 **去重邏輯**：自動處理編號與名稱重複顯示的問題。
  - 🛡️ **穩定性提升**：修復 Firestore 儲存失敗問題，並加入 API Key 安全盤查規則。

- **v0.1.0** (2026/02/11):
  - ✨ 全新 UI 優化：繽紛漸層風格，增強互動回饋。
  - 📱 RWD 改善：優化手機版填單體驗。
  - 🔄 設定遷移：Line Notify 設定改為雲端儲存 (Firestore)。
  - 🐛 修正 C118/C116 教室名稱重複導致的狀態錯誤。

---
Made with ❤️ for SMES
